---
"title": "故障排查"
---

## 故障排除

### 常见问题

1. **"command not found"**: 确保 `modix` 二进制文件在您的 PATH 中，或使用完整路径运行
2. **缺少配置文件**: 运行 `modix init` 重新创建默认配置
3. **模型切换失败**: 检查所选模型的配置是否完整
4. **API 密钥错误**: 验证 API 密钥是否正确且未过期